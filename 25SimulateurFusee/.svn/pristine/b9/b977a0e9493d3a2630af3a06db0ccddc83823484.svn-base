package interfaces;

import java.awt.Graphics2D;
import java.awt.geom.AffineTransform;
/**
 * Cette interface rend un nuage dessinable
 * @author Thomas Corbeil
 *
 */
public interface DessinableNuage {
	/**
	 * Cette méthode dessine le nuage
	 * @param g2d le contexte graphique dans lequel on veut dessinner le nuage
	 * @param mat la matrice de transformation passant du mode réel au monde des pixels
	 */
	public void dessinerNuage(Graphics2D g2d, AffineTransform mat);
	
}
